import React from "react"

export default class listkey extends React.Component{
    render(){
        return(
            <div class="container">
                list+key
            </div>
        )
    }
}