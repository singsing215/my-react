import React from "react"

export default class compose extends React.Component{
    render(){
        return(
            <div class="container">
                compose
            </div>
        )
    }
}